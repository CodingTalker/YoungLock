#include <gtest/gtest.h>

TEST(EncryptorTest, EncryptDecrypt) {
    // ...测试加密解密功能...
}

TEST(GTestSanity, Add)
{
    int a = 2;
    int b = 3;
    EXPECT_EQ(a + b, 5);
}

TEST(GTestSanity, Test)
{
    EXPECT_TRUE(true);
}